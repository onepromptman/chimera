"""Verification gates: schema gate (every artifact) + lite 3-critic REFUTE.
Lite is the only tier — the tournament flag/stub was deleted 2026-07-02
(audit F1: the flag was stored but read nowhere); a higher tier returns
only on first real high-stakes need, wired end-to-end or not at all."""
